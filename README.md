# 营业数据查询

针对现有 POS 数据库（MySQL 5.5 / PHP 8.2）的营业数据查询工具，**纯查询，不写库**。

功能：
- **登录密码保护**，密码写在 `config.php`
- 按日期区间统计营业额与人数，分**白天 / 晚上 / 全天**三档，也可只看当天
- 查询指定菜品在区间内的点单情况
- 点单最多 / 最少的 10 个菜品，以及**每个岗位**的最多 / 最少 10 个菜品
- **岗位（打印机）单量排名**
- **期间对比** —— 近 7/14/30 天 vs 前一段等长期间，或自选任意两段；
  含逐日按星期几对照，可选带上菜品与岗位的变化
- **开台核对** —— 当前已开桌未结算的台，两道检查：**人数 vs 套餐份数**、
  **每人至少一份酒水**；可人工确认并桌等例外，外带（Llevar）这类台自动免核对

菜品页的「岗位」下拉框有两种用法：
- **全部岗位**（默认）：先给出岗位汇总表，再**逐个列出每个岗位**的最多 / 最少 10 个菜品
- **选中某个岗位**：整页只统计该岗位，最多 / 最少 10 个都收窄到这个岗位内

岗位汇总表每行末尾都有「只看这个岗位」的直达链接。

菜名过长时会截断成省略号（全宽表格约 300px，岗位左右并排时约 170px），
**鼠标悬停可看到完整菜名**。截断只是显示层面的，导出或复制得到的仍是完整名称。

> 质量基线：`php tests/selftest.php` 599 项全绿（不需要数据库）。
> 另做过一轮完整体检 —— 注入、只读绕过、参数模糊、登录会话、驱动差异、
> 视图一致性、查询次数，查出的问题和结论都记在「十、体检记录」一节。

## 手机与电脑自适应

同一套页面，电脑、平板、手机都能用，不需要单独的手机版。

| 屏幕 | 表现 |
|---|---|
| ≥ 900px | 完整布局：表单横排、岗位排行左右并排 |
| 640–900px | 表单开始换行，岗位排行改为上下排列 |
| < 640px | 表单竖排、按钮通栏，导航换成短名（开台／营业额／对比／菜品／岗位）并可横滑 |

针对手机做的处理：

- **开台核对**在手机上换成一台三行的紧凑列表（桌号+状态 / 人数·套餐·酒水·时长 / 金额·菜品·服务员），
  每台约 95px —— 这页最常在店里拿手机看，14 列横向滚动没法用。
  开 20 桌约 1900px，滑两屏就能看完；桌面仍是完整表格。
  两个视图共用同一个格式化函数，不会出现口径不一致
- 其余宽表格保持横向滚动，但**首列吸附在左边**，滑动时始终知道当前是哪一天／哪道菜
- 次要列（下单次数、菜品数等）在手机上隐藏，需要时横屏或用电脑看
- 输入框字号固定 16px —— 低于这个值 iOS 会在聚焦时自动放大整个页面
- 点击目标按**指针精度**（`pointer:coarse`）而不是屏幕宽度放大，
  这样 iPad 竖屏（768px，宽于手机断点）也能得到手指友好的尺寸

不依赖任何前端框架，纯 CSS 媒体查询，没有额外的 JS。

---

## 〇、登录

所有页面都要先登录。密码在 `config.php` 的 `password` 里设置，两种写法都支持：

```php
// 1) 明文，最简单
'password' => 'abc123',

// 2) bcrypt 哈希，更安全 —— config.php 被人看到也读不出原文
//    先执行：php -r "echo password_hash('你的密码', PASSWORD_DEFAULT);"
'password' => '$2y$10$....',
```

**留空则谁都登录不进去**（是全部拒绝，不是放行）。

其他细节：
- 会话 8 小时后过期，关闭浏览器也会失效
- 会话 Cookie 设了 HttpOnly，走 HTTPS 时自动加 Secure
- 登录成功后重新生成会话 ID，防会话固定攻击
- 登录表单带 CSRF 令牌；密码比较用 `hash_equals`，密码错误时统一延迟 1 秒拖慢暴力破解
- 右上角「退出」可登出

> **注意**：登录会用到 PHP 的 session，PHP 会把会话文件写到服务器的 session 目录 ——
> 这属于 PHP 自身机制，与数据库无关，本程序对数据库仍然只读。
>
> 这是一道**基础的门禁**，适合店内网使用。它挡得住随手打开页面的人，但不是抗攻击的
> 认证系统（只有一个共享密码、没有按 IP 封禁）。放到公网请务必套 HTTPS，
> 并考虑在 Web 服务器层再加一层访问限制。

`tests/checkdb.php` 从浏览器访问时同样要求登录（它会显示数据库信息）；
命令行运行不受影响。`tests/env.php` 不要求登录 —— 它是排障刚需，
必须在任何情况下都能打开，且不显示任何业务数据。

## 一、安全性：只做查询

程序**不会**对数据库做任何写入、删除或结构变更。三道防线：

1. **代码层**：全程序只有 `lib/db.php` 一个数据库出口，只提供 `select()`，
   没有 `exec()`、没有事务、不开启多语句执行。
2. **语句层**：每条 SQL 执行前都要通过 `assertReadOnly()` —— 必须以 `SELECT` 开头，
   不得含分号，不得出现 `INSERT / UPDATE / DELETE / DROP / ALTER / TRUNCATE /
   CREATE / REPLACE / GRANT / CALL / INTO OUTFILE` 等任何写操作关键字。
   检查前会先剥掉注释（防止关键字藏在注释里绕过），并把连续空白压成一个空格
   —— 否则 `INTO␣␣OUTFILE`、`INTO\nOUTFILE` 这类变形能绕过多词规则，
   而这恰恰是清单里唯一真能往磁盘写文件的一条。另外 `LOAD_FILE` 必须单列
   （`\bLOAD\b` 匹配不到它，下划线算单词字符），`SLEEP / BENCHMARK / GET_LOCK`
   虽然不写数据但能把连接拖住，一并挡掉。
   这道检查**故意从严**：关键字出现在字符串常量里也会被拒。本程序的 SQL 全部由
   模板生成、用户输入一律参数绑定，不存在这种语句，宁可误杀也不放过。
3. **数据库层**（需要你配合）：请在 `config.php` 里填一个**只有 SELECT 权限的账号**：

   ```sql
   CREATE USER 'report_ro'@'%' IDENTIFIED BY '你的密码';
   GRANT SELECT ON coolroid.* TO 'report_ro'@'%';
   FLUSH PRIVILEGES;
   ```

   这样即使程序出任何问题，数据库也会直接拒绝写操作。

所有用户输入（日期、菜品 ID、就餐方式）一律走 PDO 参数绑定，表名走白名单校验。

## 二、安装

1. 把整个目录放到 Web 服务器可访问的位置。
2. 编辑 `config.php`，填入数据库地址、库名、只读账号密码，以及**登录密码**。
3. 浏览器打开 `index.php`，输入密码进入。

无需 Composer，无外部依赖，无需写权限（不产生任何缓存或日志文件）。

### 配置分两层（升级时很重要）

| 文件 | 放什么 | 升级时 |
|---|---|---|
| `config.php` | 数据库地址、账号密码、登录密码 | **不要覆盖** |
| `lib/settings.php` | 营业时段、套餐清单、免核对规则、酒水口径等**功能参数的默认值** | **直接覆盖** |

**升级步骤就一句话：除了 `config.php`，其它文件全传一遍。**

这么分是有教训的：`config.php` 装着密码，是你自己维护的，实际维护时基本不会
重新上传。功能参数如果只写在那边，新版本加的参数就永远读不到，
**功能会静默失效**（外带免核对就这么翻过一次车）。

想改某个功能参数时，**不要去改 `lib/settings.php`**（下次升级会被覆盖），
把同名的键写进 `config.php`，那边的值优先生效：

```php
// config.php
'day_end'         => '17:00:00',
'no_combo_tables' => ['Llevar*', '外带*', 'Barra*'],
```

`config.php` 里没写的键一律用 `lib/settings.php` 的默认值。随包的 `config.php`
把可覆盖的键都以注释形式列好了，要改哪个就解开哪一行 ——
**解开注释就等于把这个值固定下来，以后升级不再自动更新**，所以只解开真正要改的。

`tests/checkdb.php` 会告诉你当前哪些功能参数被 `config.php` 覆盖了，
「开台核对」页底部也会注明免核对规则用的是默认值还是你的配置。

#### 全部可配置项一览

默认值都在 `lib/settings.php`，要改就把同名的键写进 `config.php`。

| 键 | 默认值 | 作用 |
|---|---|---|
| `day_start` / `day_end` | `08:00:00` / `17:30:00` | 白天营业时段 `[起, 止)` |
| `night_start` / `night_end` | `18:00:00` / `02:00:00` | 晚上营业时段，跨零点 |
| `day_cut_hour` | `2` | 营业日切分点，**必须与 `night_end` 一致** |
| `max_range_days` | `92` | 查询日期跨度上限（约 3 个月），调大更吃服务器 |
| `combo_item_ids` | 6 个自助餐 ID | 算作「按人头套餐」的菜品，换菜单要更新 |
| `no_combo_tables` | `['Llevar*','外带*']` | 免核对的桌号，`*` 通配、大小写不敏感；写 `[]` = 全都核对 |
| `no_combo_eat_types` | `[]` | 按 `eat_type` 免核对，填错会把堂食也跳过，拿不准就留空 |
| `drink_print_classes` | `['bebidas*','bar*','酒水*','饮料*']` | 哪些**出品岗位**算酒水，换季加饮料不用改清单 |
| `drink_extra_item_ids` | `[]` | 岗位没覆盖到的酒水，按菜品 ID 补 |
| `drink_exclude_item_ids` | `[]` | 从酒水里剔除（免费茶水等）；**与补入冲突时以剔除为准** |
| `drink_min_per_guest` | `1` | 每人至少几份酒水；**`0` = 只统计不核对** |
| `open_table_warn_hours` | `4` | 开台超过几小时标红；**`0` 或负数 = 关掉提醒** |
| `ack_hours` | `6` | 人工确认保留多久（存 session，不写库） |
| `trend_red_up` | `false` | 对比页涨跌配色。`false` = 绿涨红跌；`true` = 红涨绿跌（股市习惯） |
| `timezone` | `Europe/Madrid` | PHP 时区，**必须与 POS 一致**；留空 `''` = 沿用 php.ini |
| `driver` | `auto` | `auto` / `pdo` / `mysqli`，一般不用管 |

`config.php` 里另有站点自己的项：`host` / `port` / `dbname` / `user` / `pass` /
`charset` / `password`（登录密码）—— 这些**只在 `config.php` 里**，不要写进
`lib/settings.php`。

### 对 PHP 扩展的要求

只需要 **`pdo_mysql` 或 `mysqli` 其中之一**，有哪个用哪个，默认的
`driver => 'auto'` 会自动选（优先 PDO，没有就用 mysqli）。两条路功能完全一致。

所以如果你的环境只装了 `mysqli`，**不需要为了这个程序去改 php.ini**。

不依赖 `mbstring`、`iconv`、`intl`、`curl`、`openssl` 等任何其他扩展。

环境诊断（完全独立，不依赖任何数据库扩展，一定跑得起来）：

```bash
php tests/env.php
```

它会报出：PHP 读的是哪个 php.ini、扩展目录在哪、目录里有哪些数据库扩展文件、
哪些扩展加载了、程序能不能跑、以及出 500 时去哪里找真正的错误。

> **命令行和 Web 服务器往往用不同的 php.ini**，所以请在浏览器里也访问一次
> `tests/env.php`，对比两边报出的路径 —— 改错文件是"改了没效果"最常见的原因。

### 时区必须对齐（很容易忽略）

程序用 **PHP 的时钟**算「今天是哪个营业日」和「这张台开了多久」，而时间数据是
**POS 写进数据库的本地时间**。两边对不上不会报错，只会让所有跟时间有关的数字
悄悄不对 —— 已开台时长偏移、「开台超 4 小时」误报、跨零点时取错营业日。

`php.ini` 里 `date.timezone` 没设时，PHP 默认走 **UTC**；西班牙是 UTC+1/+2，
于是每张台的时长都会差 1~2 小时。所以程序里显式设定：

```php
// lib/settings.php（默认值），要改就写进 config.php
'timezone' => 'Europe/Madrid',
```

留空 `''` 表示不干预、沿用 `php.ini`。

`tests/checkdb.php` 会把数据库时间和 PHP 时间一起打出来并报出差值；差值接近整小时
会直接判为**时区不一致**并报错。开台核对页如果发现有台的开台时间在「未来」，
也会在顶部横幅提示。

### 部署后先跑体检

```bash
php tests/checkdb.php
```

也可以直接用浏览器访问 `tests/checkdb.php`。它会依次检查：
PHP 扩展是否齐全、配置是否填好、数据库能否连上、账号是不是只读、
需要的 6 张表在不在、实时表有多大、数据时间范围、
以及实跑近 7 天的四条统计查询并报出耗时和结果抽样。
全程只执行 SELECT。

出问题时它会直接指出是哪一环，比在页面上看一句"查询失败"好定位得多。

### 逻辑自检（不需要连数据库）

```bash
php tests/selftest.php
```

不连数据库、不需要任何扩展，几百毫秒跑完，**改完代码先跑这个**。当前 599 项，覆盖：

| 组 | 覆盖内容 |
|---|---|
| 只读防线 | 写操作语句、多语句、注释藏关键字 |
| 只读防线·绕过尝试 | `INTO␣␣OUTFILE`／`INTO\nOUTFILE` 等空白变形、`LOAD_FILE`、`SLEEP`、`INTO @变量` |
| 真实 SQL | 程序生成的每一条 SQL 都要能通过只读检查，且参数全部走绑定 |
| mysqli 占位符 | `:name` → `?` 转换时跳过 `'08:00:00'` 里的冒号 |
| 日期范围 | 92 天上限、跨月跨年、闰年、夏令时 |
| 统计口径 | 分单人数去重、时段划分、菜品过滤、岗位归类 |
| 开台核对 | 套餐五态、酒水四态、免核对、人工确认与指纹作废、排序 |
| 期间对比 | 区间推算（跨月跨年闰年）、涨跌率、上期为 0、逐日按星期几配对、不等长、垃圾日期不抛异常、快捷天数优先级 |
| 时钟与时区 | 开台时间落在未来时的标记、`warn_hours=0`、垃圾时间字段 |
| 两种驱动一致性 | PDO 返回字符串 vs mysqli 返回原生类型，结果必须逐字节相同 |
| 配置健壮性 | 写成字符串／`null`／缺项／正则元字符／超长桌号都不能崩 |
| 页面结构 | 表格列对齐、手机与桌面两套视图、自适应关键规则没被删掉 |

## 三、统计口径

以下口径均已用你导出的 **89,139 条真实账单（2024-01-22 ~ 2026-08-13，931 个营业日）** 验证。

### 时段划分

| 时段 | 范围 | 判定依据 |
|---|---|---|
| 白天 | 08:00 – 17:30 | **开台时间** `order_start_time` |
| 晚上 | 18:00 – 次日 02:00 | 同上 |
| 时段外 | 其余时间 | 同上 |

- **营业日**：凌晨 02:00 之前的单归入**前一天**。晚市跨零点的单子不会被算到第二天。
- 全库实测：白天 51,178 单 / 晚上 37,948 单 / 时段外 **13 单**（全部在 17:30–17:53 之间）。
  时段外的单子会**单列一行**显示，不会凭空消失，保证 `全天 = 白天 + 晚上 + 时段外` 恒等。
- 已验证：**逐日单独查询之和 == 整段一次查询**，区间边界无重复、无遗漏。

时段的默认值在 `lib/settings.php`（`day_start` / `day_end` / `night_start` / `night_end`），
要改就把同名的键写进 `config.php` 覆盖。
改 `night_end` 时必须同步改 `day_cut_hour`，两者要保持一致。

### 营业额

以 **`actual_amount`（实收）** 为准。页面同时列出金额构成便于对账：

```
应收 should_amount = 原价 original_amount + 折扣 discount_amount   ← 折扣以负数记录
实收 actual_amount = 应收 should_amount   − 退单 return_amount
```

真实数据 5000 条抽样，两条恒等式 5000/5000 成立。

> **税额是价内含税**（IVA 10%），已经包含在实收里，页面单独列出仅供参考，**不要再加上去**。

### 人数

**必须按 `order_head_id` 去重。** 一张单被拆成多个 `check_id` 结账时，
每个 check 都重复写了相同的 `customer_num`。

实测（最近 90 天）：直接 `SUM(customer_num)` = 24,758 人，
按订单去重后 = **22,125 人**，直接相加会**虚高 11.9%**。

程序内层子查询按 `order_head_id` 归并：人数取 `MAX`，金额取 `SUM`，时间取 `MIN`。

### 菜品点单

明细表里混着大量非菜品行，以下全部剔除：

| 剔除对象 | 条件 | 说明 |
|---|---|---|
| 送厨房标记行 | `menu_item_id = -3` | 形如 `**999 Enviado 19:16**` |
| 付款方式行 | `menu_item_id = -4` | 形如 `EFECTIVO` |
| 退菜 | `is_return_item = 1` | |
| 套餐 / 做法子项 | `condiment_belong_item <> 0` | 实测为**负数**（如 `-77`），可用开关重新计入 |
| 做法 / 口味项 | `menu_item.item_type = 1` | 如 `S/Pepino`（不要黄瓜），共 172 个，不是菜 |
| 数量为 0 的行 | `quantity <= 0` | |

> ⚠️ **`is_send` 不是"赠送"，是"已送厨房"** —— 84% 的正常菜品都是 1。
> 千万不要拿它当赠送过滤，否则菜会被全部滤光。程序没有使用这个字段。

菜品金额 = `SUM(actual_price × quantity)`。
**不使用 `sales_amount`** —— 该字段在历史明细和实时明细里实测全是 `0` 或 `NULL`。

### 岗位

**岗位 = `print_class`（打印分类／出品部门）**，共 14 个：
Kitchen、Sushi 1–7、POS 1、POS 2、bebidas、热菜、铁板、油锅。

> ⚠️ **明细表自带的 `print_class` 字段是废的** —— 历史明细（2024-01）和实时明细（2025-07）
> 抽样中该字段全部是 `0` 或 `NULL`，从来没有真实岗位 ID。
>
> 因此岗位来自 **`menu_item.print_class`**：单独查一次 `menu_item`（666 行）和
> `print_class`（14 行）两张小字典表，在 PHP 内存里做映射，**不与大表 JOIN**。
>
> 副作用：如果某道菜后来被调整过所属岗位，历史统计会按**当前**岗位归类。

## 四、性能设计

需求要求"数据表单独统计，非必要每次只统计一张表"，实现如下：

| 页面 | 查询的表 | 说明 |
|---|---|---|
| 营业额统计 | `history_order_head`（+ 可选 `order_head`） | 无任何 JOIN |
| 菜品统计 | `history_order_detail`（+ 可选 `order_detail`） | 无任何 JOIN |
| 岗位单量排名 | `history_order_detail`（+ 可选 `order_detail`） | 无任何 JOIN |
| 开台核对 | `order_head` + `order_detail` | 各查一次，无任何 JOIN；套餐与酒水在同一条查询里算 |
| 期间对比 | 同营业额（两期各查一次） | 菜品／岗位对比默认**不查**，勾选后才扫明细表 |
| 字典 | `menu_item`、`print_class` | 各几百行，单独查询，内存映射 |

- 营业额与菜品统计**完全分开**，互不影响，不会一次拉起两张大表。
- 时间条件写成 `列 >= :from AND 列 < :to`，**不对索引列做任何函数运算**，
  可以吃满 `ids_order_start_time`（账单头）和 `idx_order_time`（明细）两个现有索引。
- 菜品排行**只查一次表**：一次取回全部菜品 × 时段的汇总（约 600 菜 × 3 时段 ≈ 1800 行），
  Top10 / Bottom10 / 各岗位排行全部在 PHP 内存里算出，不再回数据库。
  这也绕开了 MySQL 5.5 没有窗口函数、无法写 `ROW_NUMBER()` 分组取 TopN 的限制。
- 岗位单量排名同样**只查一次明细表**，且只返回十几行汇总。
  难点在于「单量」是 `COUNT(DISTINCT order_head_id)`，按菜品分别统计再相加会重复计
  （同一张单点了同岗位两个菜就会算两次），必须在数据库端按岗位分组算；
  可岗位又只能来自 `menu_item.print_class`，明细表里没有。
  解决办法是把「菜品ID → 岗位」映射编译成 SQL 里的 `CASE WHEN menu_item_id IN (…) THEN 岗位`
  表达式 —— 666 个菜品分成十几个 IN 列表，SQL 文本几 KB，MySQL 处理 IN 列表很快。
  这样既不做 JOIN、又不用把上万行明细拉回 PHP。所有 ID 都强制 `(int)` 转换后拼接。
- 日期跨度上限 92 天（约 3 个月），默认在 `lib/settings.php` 的 `max_range_days`，可在 `config.php` 覆盖。

数据量参考：931 个营业日共 89,139 单，单日最多 228 单，3 个月约 9,000 单，压力很小。

页面底部会显示每条查询的耗时，方便你确认实际表现。

## 五、⚠️ 需要你确认的一件事：实时表的数据量

"包含当天未日结数据"选项会额外查询 `order_head` / `order_detail` 两张实时表。
**这两张表没有时间索引**（只有主键），如果它们在日结后不清空、长期累积，
这个查询会变成全表扫描。

你导出的 `order_detail` 样本里有一条 `order_detail_id = 1387928`、
时间是 `2025-07-14` 的记录 —— 也就是一年多前的数据仍在实时表里。
请跑一下这条（只读）语句确认：

```sql
SELECT COUNT(*) AS rows_now,
       MIN(order_time) AS oldest,
       MAX(order_time) AS newest
FROM order_detail;
```

- **几千行以内** → 保持默认，勾选"包含当天未日结数据"没问题。
- **几十万行以上** → 建议平时**取消勾选**该选项（只查历史表），只在需要看当天实时数据时临时勾上；
  或者请 POS 厂商给 `order_detail.order_time` 和 `order_head.order_start_time` 加索引
  （这属于改表结构，本程序不会做，需要你自己决定）。

历史表 `history_order_head` / `history_order_detail` 索引齐全，不受此影响。

## 六、目录结构

```
config.php          数据库连接 + 登录密码（唯一需要你修改的文件，升级时不要覆盖）
lib/settings.php    营业时段、套餐清单、免核对规则、酒水口径等功能参数的默认值（随程序升级）
compare.php         期间对比页（本期 vs 上期）
login.php           登录页
open.php            开台核对页（人数 vs 套餐份数、每人至少一份酒水）
index.php           营业额 / 人数统计页
dish.php            菜品点单统计页
station.php         岗位（打印机）单量排名页
lib/auth.php        登录保护
lib/ack.php         开台核对的人工确认（存 session，不写数据库）
lib/db.php          只读数据库访问层（唯一的数据库出口）；两层配置合并、时区设定也在这里
lib/biz.php         SQL 构造与业务口径
lib/report.php      汇总、排行计算（纯内存，不访问数据库）
lib/view.php        页面公共组件
assets/app.css      样式
assets/app.js       菜品搜索下拉框（字典随页面下发，搜索全在浏览器完成）
tests/env.php       PHP 环境诊断（完全独立，不依赖任何扩展）
tests/checkdb.php   数据库连接与数据体检（需要数据库）
tests/selftest.php  逻辑自检（不需要数据库，599 项）
```

`lib/db.php` 里 `PdoDriver` 和 `MysqliDriver` 两个类实现同一个 `DbDriver` 接口，
上层代码只认 `Db::select()`，完全不感知底层用的是哪个扩展。
mysqli 不支持 `:name` 命名参数，驱动内部会转换成 `?` 位置参数 ——
转换时会跳过单引号字符串，避免把 `'08:00:00'` 里的冒号误当成占位符（有专门的测试覆盖）。

## 七、开台核对

对付这种情况：**开了 4 位客人，却一份套餐都没打，或者只打了 2 份** —— 直接就是漏收钱。

页面列出当前**已开桌、未结算**的每一台，逐台比对开台人数与已打的套餐份数：

| 核对结果 | 含义 |
|---|---|
| 一致 | 套餐份数 = 人数 |
| 套餐打少了 | 份数比人数少，**大概率漏打** |
| 未打套餐 | 一份都没打 |
| 套餐打多了 | 份数比人数多 |
| 未填人数 | 开台时没填人数，无法比对 |
| 免核对 | 外带（Llevar）这类台，本来就没有人头套餐，不参与核对 |

同一页还并行做**第二道检查：每人至少一份酒水**（详见下面「酒水核对」）。
两道检查各有各的徽章，任意一道不合格，这张台就进「需要核对的台」。

**排序：套餐有问题的最前，其次是只有酒水不足的，然后是已人工确认的、全都合格的，
免核对的排最后；每一档内部按桌号排**
（自然排序，2 排在 10 前面，纯数字桌号排在 `Llevar` 这类文字桌号之前），
方便照着桌号一台台核。同时给出「已点菜品」「当前金额」「已开台时长」帮助判断 ——
刚开台还没下单的台也会显示成「未打套餐」，看时长和菜品数就能区分。
开台超过 `open_table_warn_hours`（默认 4，在 `lib/settings.php`）小时仍未结账的会把时长标红，
方便发现忘了关的台。

> **只点单品、不吃自助的客人本来就不会有套餐**，这类会显示成「未打套餐」，属正常。
> 请结合金额和菜品数判断，不要一律当成漏打。

### 酒水核对：每人至少一份

规则是**「至少」，不是「等于」** —— 2 位客人点 2 份就算达标，点 3 份、5 份都行，
只点 1 份才会提示。

| 核对结果 | 含义 |
|---|---|
| （无徽章） | 达标，不打扰 |
| 酒水不足 | 点了，但少于人数；会标出**还差几份** |
| 未点酒水 | 一份都没点 |

每张台都会列出**酒水杯数与金额**，汇总卡片给出全场的酒水份数、金额、与人数的差额。

#### 怎么判定哪些菜品算酒水

**按出品岗位（`print_class`）判定，不是一个个列菜品 ID** —— 酒水都从吧台／饮料机出，
岗位就是现成的分类，换季加了新饮料也不用回来改清单，只要它挂在这些岗位下就会自动算进来。

```php
// lib/settings.php 的默认值，要改就写进 config.php
'drink_print_classes' => ['bebidas*', 'bar*', '酒水*', '饮料*'],
```

岗位名匹配，大小写不敏感，`*` 是通配符。**核对页底部会列出实际命中了哪几个岗位、
涵盖多少个菜品**，一个都没命中时会明确报出来并列出本店的岗位名，照着填即可。

个别情况可以微调（都在 `lib/settings.php`，`config.php` 里覆盖）：

```php
'drink_extra_item_ids'   => [],   // 岗位没覆盖到的酒水，按菜品 ID 补进来
'drink_exclude_item_ids' => [],   // 免费茶水、开水之类，从酒水里剔除
'drink_min_per_guest'    => 1,    // 每人至少几份；0 = 只统计杯数金额，不核对
```

补入与剔除冲突时**以剔除为准**。做法／口味项（`item_type = 1`）永远不算酒水。

> 酒水份数和套餐份数是**同一条 `order_detail` 查询**用 `SUM(CASE WHEN …)` 一起算出来的，
> 多这一项不增加任何扫描量，仍然是「一次开台核对只扫两张实时表各一遍」。

### 外带等不用核对的台

外带（Llevar）没有堂食人数，也不会点按人头的自助套餐，拿人数去比对只会一直报
「未打套餐」，纯属噪音。这类台判为**免核对**：不计入「需要核对的台」，
在列表里压暗排到最后，也不提供「确认」按钮（服务端同样拒绝对它盖章）。

默认规则在 `lib/settings.php`，按**桌号**匹配，大小写不敏感，`*` 是通配符：

```php
'no_combo_tables' => ['Llevar*', '外带*'],
```

`Llevar*` 能匹配 `Llevar`、`LLEVAR 2`、`llevar-03`。要再排除吧台、打包台，
加一行 `'Barra*'` 之类即可。匹配是**整体匹配**，`A-Llevar` 不会命中。

> 要改就在 `config.php` 里写同名的键覆盖（见「配置分两层」）。
> 页面底部会注明当前用的是默认值还是你的配置。
> 真要「所有台都核对」，在 `config.php` 里明确写成空数组 `[]`。

也可以按 `order_head.eat_type` 排除（比桌号更准，但要先确认本店的取值），
在 `config.php` 里写：

```php
'no_combo_eat_types' => [3],   // 默认留空 []，拿不准就别动
```

> ⚠️ `eat_type` 填错会把堂食台也一并跳过。改完先到核对页看看「免核对」的台数对不对 ——
> 页面底部会列出当前生效的两条规则，卡片上也会显示免核对的台数。
>
> 新增外带台、改了桌号命名规则后记得回来更新，否则那些台会一直报「未打套餐」。

### 人工确认（并桌之类的情况）

并桌会让一张单人数很多却没有套餐，核对结果显示成「未打套餐」，但其实是正常的；
「这桌就是不喝酒」也一样。这类台可以点「确认」标记掉，之后不再计入「需要核对的台」。

- **要点两下**：先点「确认」，该行原地展开「确定？[确定][取消]」，再点一次才生效 ——
  防止手机上误触。整个流程走服务端，不依赖 JS
- **存在当前登录会话里**（PHP session），**不写数据库** —— 本程序始终只读
- 默认 `ack_hours`（6，在 `lib/settings.php`）小时后自动失效；**退出登录即清空**；换一台设备要重新确认
- **该台人数、套餐份数、或酒水够不够，任一项变化，确认立即作废**，重新回到待核对状态 ——
  补打了套餐、加了人，都不会被旧的确认盖住
- 酒水只记「**够没够**」而不记杯数 —— 规则是「至少」，所以不足时又加一杯（仍然不足）
  不会让确认作废，否则一直在重复确认
- 只是又点了几个菜、金额变了，确认**仍然有效**（否则会一直重复确认）
- 顶部有「全部撤销」，可一次清空

> 确认是按浏览器存的：经理手机上确认过，收银台电脑上仍会提示。
> 这是 session／cookie 这类客户端方案的固有限制 ——
> 要跨设备共享就必须落库，那会打破只读约束。

技术细节：确认时记下当时的「人数:套餐份数:酒水是否达标」指纹，渲染页面时比对，
对不上就当没确认过。
提交走 POST + CSRF 校验，并且会再次比对提交上来的指纹与服务端当前值 ——
如果从页面渲染到点确定之间数据变了，会拒绝并提示重新核对，不会拿旧状态盖章。

### 哪些菜品算「按人头的套餐」

由 `combo_item_ids` 决定（默认在 `lib/settings.php`，可在 `config.php` 覆盖），
默认是从菜单表查出来的 6 个自助餐套餐：

```php
'combo_item_ids' => [1290, 1490, 1590, 1890, 2390, 2590],
```

成人餐和儿童餐都要列进去 —— 4 位客人可能是 2 大 2 小，加起来仍应是 4 份。

BOX 1–18 与 COMBO S/M/L/XL（`item_type = 3`）**默认不计入**：那是打包组合，
不是按人头点的，算进来会让核对失真。

> ⚠️ **换季改菜单、新增套餐后记得更新这个清单**，否则核对结果会失真。
> 页面底部会折叠列出当前生效的清单和各自单价，其中在菜单表里查不到的 ID 会被标出来。

### 数据来源

只查两张实时表，各一次，不做 JOIN：`order_head` 拿开台列表，
`order_detail` 一次算出每台的套餐份数与菜品总数。

「未结算」判定为 `order_end_time IS NULL`。如果这个假设在你的系统里不成立
（页面始终空白），把范围切到「实时表里的全部订单」就能看到实际情况，
该模式会多一列显示每单是否已结算。

> `order_detail` 没有 `order_head_id` 索引，这条查询会全表扫描一次。
> 实时表通常只装当前未日结的数据，问题不大；若该表很大（见第五节），这一页会偏慢。

## 七之二、期间对比

回答「这周比上周好还是差」这类问题。两种用法：

### 快捷：近 7 天 vs 前 7 天

**今天周三**，那就是：

| | 区间 | 天数 |
|---|---|---|
| 本期 | 上周四 → **今天（周三）** | 7 |
| 上期 | 上上周四 → 上周三 | 7 |

两段首尾相接、都是 7 天，所以**星期几自动对齐** —— 周四对周四、周三对周三。
餐饮的周末和平日差得远，对齐了才有可比性。还有近 14 天、近 30 天两档。

> ⚠️ **本期含今天，而今天还没营业完** —— 拿半天比上期完整的一天，数字天然偏低。
> 页面会提示这一点。想比完整的天，把本期结束日改成昨天。

### 自选：两种填法

- **只填本期**：对比期自动取紧挨着的、等长的前一段。
  填 8/1–8/31，自动对比 7/1–7/31。
- **勾「手动指定对比期」**：四个日期自己填，比如 `2026-08` 对 `2025-08`（去年同月）。

两期天数不一样时页面会警告，并让你重点看**日均**（合计数直接比会失真）。

### 页面内容

1. **概览卡片**：营业额、人数、单数、人均、单均 —— 每张都给出本期、上期、涨跌额与涨跌率、日均
2. **分时段对比**：白天／晚上／全天各自的营业额、人数、单数、人均
3. **逐日对照**：本期第 1 天对上期第 1 天，两列都标出星期几，一眼看出是哪天拖了后腿
4. **菜品点单量变化**（可选）：卖得更多／更少各 15 个
5. **岗位单量变化**（可选）：各岗位的单量涨跌

### 涨跌怎么标

**绿涨红跌**（默认），而且**不只靠颜色**：

```
▲ +420.00 25.0%     涨（绿底）
▼ −2 33.3%          跌（红底）
— 持平              没变（灰底）
```

三重冗余：**箭头 ▲▼ + 正负号 + 颜色**。约 8% 的男性有红绿色觉障碍，只靠颜色
他们分不出来；样式表万一没加载，光看文字也知道是涨是跌。

为什么默认绿涨红跌而不是股市那套红涨绿跌：**本程序里红色自始至终表示「要处理的事」**
—— 未打套餐、酒水不足、开台超时、错误提示，全是红的。如果再让「营业额涨了」也是红的，
同一个红色在同一个页面里就有了两种相反的含义，扫一眼很容易读反。

习惯红涨绿跌的话，在 `config.php` 里加一行即可，**只翻转颜色，箭头方向不变**
（▲ 永远表示涨）：

```php
'trend_red_up' => true,
```

### 口径说明

- 涨跌率 = （本期 − 上期）÷ 上期。**上期为 0 时算不出百分比**，显示成「新增」
  而不是硬算成 100% 或 ∞ —— 那两种写法都会误导人。
- **人均、单均是各期各自算完再比**，不是把差额相除。
  总额涨了 25%、人也多了 25%，人均是持平的，页面会如实显示「持平」。
- 逐日对照按**位置**配对（本期第 1 天对上期第 1 天）。两期等长时位置对齐就等于
  星期几对齐；自选出不等长的两段时，多出来的几天标成「无对应」，不硬凑，
  并且会提示星期几没对上。
- 菜品变化按**点单量的绝对变化**排序，不是按百分比 —— 一个菜从 1 份涨到 3 份是
  +200%，但没什么意义。

### 查询开销

营业额对比 = 账单头表查两次（本期一次、上期一次），仍然不做任何 JOIN。
**菜品和岗位对比要扫明细大表，所以默认关着**，需要时在页面上勾选。
勾上两项后一次对比最多 8 条查询（两期 × 账单头／明细／岗位，加两张字典表）。

## 八、岗位单量排名

「单量」= 该岗位出品**涉及了多少张单**（多少桌）。一张单里同岗位点了几个菜也只算一单。

同时给出这几个指标，可任选一个作为排名依据：

| 指标 | 含义 |
|---|---|
| 单量 | 该岗位涉及多少张单（`COUNT(DISTINCT order_head_id)`） |
| 出品份数 | 数量合计（`SUM(quantity)`） |
| 出品笔数 | 明细行数 |
| 金额 | `SUM(actual_price × quantity)` |

并按白天 / 晚上 / 时段外拆开显示。

> **各岗位单量之和会大于总账单数**，这是正常的 —— 一张单通常会经过多个岗位
> （比如既有热菜又有饮料），每个岗位都会各自计一单。要看真实的总账单数，
> 请以「营业额统计」页为准。
>
> 同理，「菜品数」是各岗位各自的去重菜品数，也不能相加，所以合计行留空。

两类特殊分组会单独成行，不和正常岗位混在一起：

- **未分配岗位** —— 菜品在 `menu_item` 里存在但没配 `print_class`
- **菜品已从菜单删除** —— 历史订单里有这个菜品 ID，但 `menu_item` 里已经查不到了

## 九、已知口径说明

- `status` 字段全库只有一个值 `2`，无法用来区分作废单，程序**不做**该字段过滤。
- `rvc_center` 只有一个营业点（`Comedor`），因此没有做营业点维度。
  将来若开分店，需要在 `Biz::buildSalesSql()` 里加 `rvc_center_id` 分组。
- `eat_type`：`0` = 堂食（78,535 单），`3` = 外带 `Llevar`（10,597 单），
  另有 `1`/`2` 共 7 单。页面提供筛选，默认统计全部。
- 全库有 1,672 张 `actual_amount = 0` 的账单（占 1.9%，多为开台未消费）。
  默认计入账单数，可勾选"排除 0 元账单"剔除。
- **自助餐套餐的 `item_type` 是 `0` 而不是 `3`** —— 只有 BOX 1–18 和
  COMBO S/M/L/XL 是 `3`。所以套餐清单只能按菜品 ID 列，不能按 `item_type` 筛。
  （这也是 Pad 上「套餐显示 0 份」的原因，已用 4 张实物小票核对过金额。）
- **酒水按出品岗位判定**，靠的是 `menu_item.print_class`。副作用与岗位统计相同：
  某道菜后来被调整过所属岗位的话，会按**当前**岗位归类。
- 开台核对的「未结算」判定依据是 `order_end_time IS NULL`。如果核对页始终是空的，
  把范围切到「实时表里的全部订单」看看实际情况 —— 说明这个假设在你的系统里不成立。
- 人工确认存在 **PHP session** 里，因此**按浏览器隔离**：经理手机上确认过，
  收银台电脑上仍会提示。要跨设备共享就必须落库，那会打破只读约束。

## 十、体检记录：查出来的问题与注意事项

以测试员视角做过一轮完整体检 —— 恶意数据注入、只读防线绕过、参数模糊测试
（178 次畸形请求）、数值边界、登录会话、两种驱动的返回类型、手机与桌面视图
一致性、每页查询次数。以下是**查出来的真实问题**，都已修复，记在这里是因为
它们代表几类容易重犯的坑。

### 1. 只读防线被空白变形绕过（严重，已修）

`INTO OUTFILE` 原来是当普通关键字匹配的，正则里写死了中间**恰好一个空格**：

```sql
SELECT * FROM t INTO OUTFILE '/tmp/x'    -- 拦住了
SELECT * FROM t INTO  OUTFILE '/tmp/x'   -- 两个空格，溜过去了
SELECT * FROM t INTO
OUTFILE '/tmp/x'                          -- 换行，也溜过去了
```

而这**恰恰是禁用清单里唯一真能往磁盘写文件的一条**。现在检查前会先把连续空白
压成一个空格，多词构造（`INTO OUTFILE`／`INTO DUMPFILE`／`INTO @变量`／
`PROCEDURE ANALYSE`）改走独立的正则规则。

> 教训：**多词关键字不能按字面量匹配**。SQL 里的空白是自由的。

### 2. `LOAD_FILE` 从 `LOAD` 底下溜过去（已修）

清单里有 `LOAD`，用的是 `\bLOAD\b` 词边界匹配 —— 但**下划线是单词字符**，
所以 `LOAD_FILE` 根本匹配不到。同类的还有 `SLEEP`、`BENCHMARK`、`GET_LOCK`
（不写数据，但能把数据库连接拖住不放）。都已单列。

> 这两条的实际风险都不高：程序的 SQL 全部由模板生成，用户输入一律参数绑定，
> 根本没有把外部内容拼进 SQL 的路径；只读账号也没有 `FILE` 权限。
> 但 `assertReadOnly()` 是**兜底防线**，兜底的东西破了洞就没有意义。

这道检查现在是**故意从严**的：关键字出现在字符串常量里（`WHERE a = 'INTO OUTFILE'`）
也会被拒。程序里不存在这种语句，宁可误杀（查询被拒，看得见）也不放过。

### 3. 没有时区控制（严重，已修）

程序用 **PHP 的时钟**算「这张台开了多久」，数据却是 **POS 写进数据库的本地时间**。
`php.ini` 没设 `date.timezone` 时 PHP 默认走 **UTC**，西班牙是 UTC+1/+2 ——

- 每张台的「已开台」时长差 1~2 小时
- 「开台超 4 小时」的提醒跟着误报／漏报
- 跨零点时「今天」可能取到错的营业日

**最麻烦的是它不报错**，页面照常显示，只是数字悄悄不对。体检时构造「开台时间在
未来」的数据，发现时长会渲染成 `0:-45` 这种鬼东西 —— 才把整条链路挖出来。

现在：`lib/settings.php` 有 `timezone` 项（默认 `Europe/Madrid`）并在读配置时套用；
开台时间落在未来时页面顶部横幅明确提示；`tests/checkdb.php` 报出数据库与 PHP 的
时钟差，**差值接近整小时就直接判为时区问题**并给出改法。

> 部署到新机器后，**第一件事是跑一次 `php tests/checkdb.php` 看时钟那一行**。

### 4. 菜品字典 JSON 内嵌 `<script>` 未转义尖括号（已修）

菜品搜索框的字典是内嵌在 `<script>` 里下发的。`json_encode()` 默认会转义 `/`
（所以 `</script>` 变成 `<\/script>`，标签截不断），但**不转义 `<` 和 `>`** ——
菜名里若有 `<script>` 或 `<!--` 就会原样落进脚本块。

实测在 Chromium 里没有被利用（脚本没执行、DOM 没被破坏），属于「离出事只差一步」。
已加 `JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT`，
尖括号全部转成 `\uXXXX`。

> **菜名、桌号、服务员名都是店员随手填的，一律当不可信数据处理。**
> 页面上其余位置全部走 `h()`（`htmlspecialchars` + `ENT_QUOTES` + `ENT_SUBSTITUTE`），
> 体检时用真浏览器验证过：注入的脚本一个都没执行。

### 5. `open_table_warn_hours = 0` 会把每张台都标红（已修）

字面理解「超过 0 小时就提醒」没错，但显然不是想要的意思，而且与酒水的
`drink_min_per_guest = 0`（表示关闭核对）约定不一致。现在 **0 或负数 = 关掉提醒**。

### 6. 测试脚本自己"空跑通过"（工具问题，已修）

加酒水列时漏写了「差额」单元格，桌面表格整行左移一列 —— 但表格对齐检查脚本
当时**没传入文件参数**，空跑一圈报了"通过"。现在该脚本没有输入时直接报错退出。

> 教训：**检查脚本必须在"什么都没检查"时失败，而不是通过。**
> 同类的坑还踩过好几次：用 `grep '一致'` 之类的关键词做断言，结果匹配到了页面
> 底部的图例说明而不是真实数据行。断言要锚定到只有数据行才有的标记上。

### 7. 快捷对比的下拉框切了等于没切（已修）

「期间对比」页的判断原来写成「`preset` 非空**且**没传 `start` 才按天数算」。
可日期框本来就会跟着表单一起提交 —— 用户把下拉从 7 天切到 30 天时，浏览器同时
带上了旧的 `start/end`，于是永远走进 else 分支，**切了等于没切**。

我的 smoke 测试当时是直接拼 `?go=1&preset=30`（没带 `start`）才蒙混过去的。
现在规则改成**快捷天数优先**：要自选日期就把下拉切到「自选日期」，
而且手改日期时会自动把下拉切回自选。

> 教训：**测试要模拟表单真实提交的全部字段**，不能只拼自己关心的那几个参数。

### 8. 垃圾日期让对比页白屏（已修）

`start=abc` 时 `strtotime()` 返回 `false`，在 `declare(strict_types=1)` 下传给
`date()` 会抛 `TypeError`。而算「上期」的 `prevRange()` 在页面上是**先于**日期校验
调用的（要拿它填表单默认值），异常抛出去就是白屏 —— 连「日期格式不正确」都看不到。

现在 `prevRange` / `dateList` / `lastDays` 遇到解析不了的日期一律原样返回，
交给 `validateRange()` 去正常报错。

### 体检确认没问题的部分

| 检查项 | 结果 |
|---|---|
| 恶意菜名／桌号／员工名注入（`<script>`、`onerror=`、非法 UTF-8、超长串） | 4 个页面均无脚本执行、无属性注入、无 PHP 报错泄漏；真浏览器验证 |
| 参数模糊测试（178 次：畸形日期、超大数、负数、`NULL` 字节、数组形式、缺参数） | 无 500、无报错泄漏、无渲染中断 |
| 登录与会话（18 项） | 未登录全部拦截、CSRF 生效、登录后重建会话 ID、Cookie 为 HttpOnly + SameSite + 自定义名、退出立即失效、伪造 session 无效 |
| PDO（返回字符串）与 mysqli（返回原生类型） | 开台核对／营业额／菜品／岗位四条链路结果逐字节一致 |
| 手机列表 vs 桌面表格 | 每台的人数／套餐／酒水／徽章／确认按钮逐项一致，顺序一致 |
| 每页查询次数 | 开台 4 次、营业额 1 次、菜品 3 次、岗位 3 次；无 JOIN、无 N+1，最大 SQL 3.6 KB |
| 数值边界 | 0 人／0 单不除零，负数、`NAN`、`INF`、浮点误差（0.1+0.2）均正常 |
| 配置手滑写法 | 写成字符串／`null`／缺项／正则元字符／超长桌号都不崩 |

### 日常维护要注意的几件事

1. **升级只覆盖 `config.php` 以外的文件**（见「配置分两层」）。功能参数写在
   `lib/settings.php` 里就是为了让新功能能自动生效。
2. **换季改菜单后回来更新两个清单**：`combo_item_ids`（按人头套餐）和
   `drink_print_classes`（酒水岗位）。开台核对页底部会列出当前生效的内容，
   点开看一眼就知道对不对。
3. **换服务器后先跑 `php tests/checkdb.php`**，重点看时钟那一行和权限那一行。
4. **改完代码先跑 `php tests/selftest.php`**，不连数据库、几百毫秒出结果。
5. 数据库账号请始终用**只读账号**。程序的三道防线里，只有这一道是数据库强制的。

## 十一、常见问题

**`Undefined constant PDO::MYSQL_ATTR_MULTI_STATEMENTS`**

这个常量由 pdo_mysql 扩展注册，扩展没加载时它就不存在。程序已改成
`defined()` 判断后再使用，缺了不影响功能 —— 真正拦截多语句的是 SQL 里的分号检查。

**`could not find driver` / 没有可用的 MySQL 扩展**

pdo_mysql 和 mysqli 都没加载。**启用 `extension=mysqli` 就够了**，不必启用 pdo_mysql。

**启用 `extension=pdo_mysql` 后 Web 服务器返回 500**

不用管它 —— 把这行改回注释状态，只要 `extension=mysqli` 是启用的，程序照常工作。

如果你确实想查 500 的原因，跑 `php tests/env.php`，它会列出扩展目录里实际有哪些
文件。Windows 上最常见的原因是 `C:\php…\ext\php_pdo_mysql.dll` 不存在，
或者 dll 的线程安全模式（TS/NTS）与 PHP 本体不匹配。

**页面能打开但查不出数据**

先跑 `php tests/checkdb.php`，第 7 节会显示数据库里实际的数据时间范围 ——
很可能是查询的日期超出了这个范围。

**「已开台」时长明显不对，或者一堆台莫名其妙被标红**

八成是**时区**：PHP 和 POS 数据库的时钟不一致。跑 `php tests/checkdb.php`
看「时钟」那一行 —— 差值接近整小时就是时区问题，在 `config.php` 里把
`timezone` 设成 POS 所在时区即可。详见「时区必须对齐」一节。

如果开台核对页顶部出现红色横幅说「有 N 张台的开台时间在未来」，那就是同一个问题。

**新加的功能在我这儿没生效**

多半是升级时没覆盖 `lib/settings.php`，或者你的 `config.php` 里还留着旧的同名键
把默认值盖住了。跑 `php tests/checkdb.php`，第 2 节会列出
「config.php 覆盖了哪些功能参数」—— 不该出现在里面的键，从 `config.php` 里删掉即可。

**外带（Llevar）还在报「未打套餐」**

看开台核对页底部的「免核对 当前的判定规则」，确认你的桌号能被规则匹配上。
规则是**整体匹配**：`Llevar*` 能匹配 `Llevar 2`，但匹配不了 `A-Llevar`。

**酒水一直显示 0**

看开台核对页底部的「酒水口径」那一段。如果写着「当前一个岗位都没命中」，
它会把你店里实际的岗位名列出来，照着填进 `config.php` 的 `drink_print_classes` 即可。
