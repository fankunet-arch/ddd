# 注意事项 —— 改动本程序前必读

> 这份文档记的是**不能破的规矩**，不是建议。
> 每一条后面都写了「为什么」和「破了会怎样」—— 因为只写「不要这样做」，
> 半年后接手的人（包括你自己）会觉得没道理，然后就改了。
>
> 能用程序检查的规矩，已经写进 `tests/selftest.php` 的【0】铁律一组，
> 破了会直接测试失败。光靠文档是记不住的。

---

## 铁律一：绝对不碰主数据库

**规矩**：本程序对 POS 主库（`coolroid`）**只允许 SELECT**，
不允许任何形式的写入、删除、改表结构。

**为什么**：这是立项时定下的第一条约束。主库是餐厅正在用的生产库，
POS 机、Pad、打印机全靠它吃饭。这个查询工具挂了没关系，主库被写坏了是事故。

**具体要求**：

1. **全程序只有 `lib/db.php` 一个数据库出口**。
   不许在别的文件里 `new PDO(...)`、`new mysqli(...)`、`mysqli_connect(...)`。
2. `Db` 类**只提供 `select()` / `selectOne()`**，不提供 `exec()`、不开事务、
   不开多语句执行。
3. 每条 SQL 执行前必须过 `Db::assertReadOnly()`。这道检查**故意从严**，
   宁可误杀也不放过 —— 关键字出现在字符串常量里也会被拒。
4. `config.php` 里请填**只有 SELECT 权限的账号**。这是三道防线里唯一
   由数据库强制的一道，前两道都在代码里，代码是可能被改的。

**破了会怎样**：这条破了，这个项目就失去了存在的前提。

> ⚠️ 改 `lib/db.php` 时格外小心。体检时发现过 `INTO OUTFILE` 能被
> `INTO␣␣OUTFILE`（两个空格）绕过去 —— 那是禁用清单里唯一真能往磁盘写文件的一条。
> 详见 README「十、体检记录」。

---

## 铁律二：自有数据和主库彻底分开

**已经落地了**（肉类采购记录），当前的分工是：

| | 类 | 连什么 | 权限 |
|---|---|---|---|
| POS 主库 | `Db`（`lib/db.php`） | MySQL | **只读** |
| 自有数据 | `Store`（`lib/store.php`） | SQLite 文件 | 可读可写 |

**下面每一条都是硬性的**：

1. **两个连接、两个类，物理隔离。**
   两个类互不引用：`Db` 不认识 `Store`，`Store` 里一个 `mysql` 字样都没有
   （构造时强制检查 DSN 必须是 `sqlite:`）。不许让 `Db` 长出写能力。

2. **绝不在同一段代码里同时操作两边。**
   页面里可以先查主库、再查自有存储，但**不要写出「一个函数里既读主库又写自有库」
   的代码**。一旦混在一起，将来改动时很容易顺手把写操作接到主库连接上。

3. **跨源统计只能在 PHP 内存里合并，不许 JOIN。**
   SQLite 和 MySQL 之间没法 JOIN；就算将来自有数据放在同一个 MySQL 实例上、
   技术上能跨库 JOIN，**也不要这么写** —— 那等于把主库和自有库绑在一条 SQL 里，
   隔离就名存实亡了。
   合并的做法程序里已有先例：菜品字典、岗位字典都是单独查出来在内存里映射的。

4. **存储文件绝不能放在网站根目录下。**
   SQLite 的 `.db` 文件放在 web 目录里，别人直接输 URL 就能整个下载走。
   放到网站目录**外面**，或者用 `.htaccess` / IIS 规则挡死。

5. **写语句只允许出现在 `lib/store.php` 和 `lib/meat.php` 里。**
   别处一旦冒出 `INSERT` / `UPDATE` / `CREATE TABLE`，就说明有人把写操作
   接到了主库那条线上 —— 自检脚本会直接报错。
   反过来，这两个文件也**不许引用 `Db::select`**。

6. **写权限只给数据目录那一处。**
   查询功能仍然零写入；只有采购记录要写它自己的 SQLite 文件。
   权限不对时页面会说清楚是哪个目录、要怎么改，不会白屏。

**为什么写得这么细**：因为「分开」这件事在第一天最容易做到，
之后每加一个功能都会有「就这一次，混一下更省事」的诱惑。混过一次就回不去了。

---

## 铁律三：配置分两层，功能默认值不能只写在 `config.php`

| 文件 | 放什么 | 升级时 |
|---|---|---|
| `config.php` | 数据库地址、账号密码、登录密码 | **不要覆盖** |
| `lib/settings.php` | 所有功能参数的默认值 | **直接覆盖** |

**规矩**：新加的功能参数，默认值**必须**写进 `lib/settings.php`，
`config.php` 里最多以注释形式列出来。

**为什么**：`config.php` 装着密码，是站点自己维护的，实际维护时基本不会重新上传。
功能参数只写在那边，新版本加的参数就永远读不到。

**破了会怎样**：功能**静默失效** —— 不报错，页面照常打开，功能就是不生效。
「外带免核对」翻过一次这个车：默认值只写在 `config.php`，
用户沿用旧配置，Llevar 照样报「未打套餐」，从表面完全看不出问题在哪。

---

## 铁律四：PHP 的时区必须和 POS 一致

**规矩**：`lib/settings.php` 的 `timezone` 必须设成 POS 所在时区（默认 `Europe/Madrid`）。

**为什么**：程序用 **PHP 的时钟**算「今天是哪个营业日」「这张台开了多久」，
数据却是 **POS 写进数据库的本地时间**。`php.ini` 没设 `date.timezone` 时
PHP 默认走 UTC，西班牙差 1~2 小时。

**破了会怎样**：**不报错**，只是所有跟时间有关的数字悄悄不对 ——
已开台时长偏移、「开台超 4 小时」误报、跨零点取错营业日。
这是最难自己发现的一类问题。

**换服务器后第一件事**：跑 `php tests/checkdb.php`，看「时钟」那一行。

---

## 铁律五：查询开销 —— 每次只统计一张表

**规矩**（立项时的原话：「数据表单独统计，非必要尽可能每次只统计一张数据表」）：

1. **不做任何 JOIN**。字典表（`menu_item` / `print_class`，几百行）单独查，
   在 PHP 内存里映射。
2. 时间条件写成 `列 >= :from AND 列 < :to`，**不对索引列套函数**，
   否则用不上现有索引。
3. **要扫明细大表的功能默认关着**（对比页的菜品／岗位对比），勾选后才查。
4. 日期跨度上限 92 天，别随手调大。

**破了会怎样**：`history_order_detail` 有 138 万行且没有 `order_head_id` 索引。
一条写歪的 SQL 能把生产库拖住 —— 而那是餐厅正在用的库。

---

## 铁律六：这些统计口径是踩坑踩出来的，别想当然改

| 口径 | 正确做法 | 想当然会怎样 |
|---|---|---|
| **人数** | 必须按 `order_head_id` 去重（`MAX(customer_num)`） | 直接 `SUM` 会虚高 **11.9%**（分单时每个 check 都重复写了人数） |
| **`is_send`** | **不是「赠送」，是「已送厨房」**，程序不使用它 | 拿它当赠送过滤，84% 的正常菜会被滤光 |
| **套餐** | 自助餐套餐 `item_type = 0`，**不是 3** | 按 `item_type = 3` 筛，套餐永远是 0 份（Pad 上就是这么显示的） |
| **明细表的 `print_class`** | 是废的，全为 `0`/`NULL`；岗位只能来自 `menu_item.print_class` | 直接用明细表的字段，岗位统计全空 |
| **菜品金额** | `SUM(actual_price × quantity)` | 用 `sales_amount`，该字段实测全是 `0`/`NULL` |
| **税额** | 价内含税，已包含在实收里 | 再加一次，营业额虚高 |
| **营业日** | 凌晨 2 点前归前一天 | 晚市跨零点的单被算到第二天 |

改这些之前，先去 README「三、统计口径」看验证数据 —— 每条都是拿真实账单核过的。

**自有数据（肉类周报表）这边也有几条，同样别想当然改**：

| 口径 | 正确做法 | 想当然会怎样 |
|---|---|---|
| **采购 ≠ 消耗** | 页面必须写明统计的是**采购量**，并给出「近 4 周滚动平均」 | 一开始备货，某周进一大批就显得「浪费」，其实只是进货节奏 |
| **滚动平均遇到没数据的周** | **跳过**，不参与平均 | 当成 0 会把均值拉低，下一周于是成了假的「暴涨」 |
| **第一条采购记录之前的周** | 标「尚无记录」，**不进分母** | 那不是「那周没买肉」而是「还没开始记」；算进去周均被摊薄、成本占比虚低 |
| **中间某周没进货** | **照常计入** | 那种才可能是真的靠库存撑过去了，抹掉就看不出备货节奏 |
| **本周（没过完）** | 标「本周未完」，不进合计 | 半周数据会把周均和占比拉低 |
| **一条记录都没有的周** | 人均、占比显示 `—` | 显示 `0 g/人` 会被读成「一点肉都没用」，和「没记」差得很远 |
| **缺重量或缺总价的记录** | 不进合计，行尾标 `!` 说明有几条没算 | 悄悄按 0 计，合计偏低而没人知道 |
| **条数折公斤** | 中位数、样本 ≥ 3 条、单位必须一致，估算值标 `≈` | 拿两三条样本算系数，或把「条」的系数套到「包」上 |

---

## 铁律七：界面上不能只靠颜色传达信息

**规矩**：涨跌、状态这类信息，颜色之外必须还有**文字或符号**。

现在的做法：涨跌是「▲ + 数字 + 颜色」三重冗余；状态是文字徽章。

**为什么**：约 8% 的男性有红绿色觉障碍；样式表没加载时页面也得能读。

**另外**：本程序里**红色自始至终表示「要处理的事」**（未打套餐、酒水不足、
开台超时、错误提示）。不要让红色再有第二种含义 —— 这就是涨跌默认用
「绿涨红跌」而不是股市那套「红涨绿跌」的原因（想改的话有
`trend_red_up` 开关，只翻颜色不翻箭头）。

**还有两条容易忘的**：
- 输入框字号**不得低于 16px** —— 低于这个值 iOS 会在聚焦时自动放大整个页面
- 表格里数字列的 `<th>` 必须带 `class="n"`，否则表头左对齐、数字右对齐，看着错位

---

## 铁律八：手机视图和桌面视图必须是同一份数据的两种画法

开台核对页有两套渲染（手机紧凑列表 / 桌面完整表格）。

**规矩**：两者**共用同一个格式化函数**（`$fmt`），不许各写各的。

**破了会怎样**：两个视图的口径会慢慢跑偏，同一张台在手机上和电脑上显示不同的数字，
而且没人会发现 —— 因为很少有人同时看两边。

---

## 测试纪律（这几条是踩过坑总结的）

1. **改完代码先跑 `php tests/selftest.php`**，不连数据库、几百毫秒出结果。

2. **检查脚本必须在「什么都没检查」时失败，而不是通过。**
   表格对齐检查脚本曾经在没传文件参数时空跑一圈报「通过」，
   于是放过了一个整行错位一列的 bug。

3. **断言要锚定只有数据行才有的标记**，不要用普通关键词。
   踩过好几次：用 `grep '一致'` 结果匹配到了页面底部的图例说明；
   用正则找「酒水」结果先命中了「未点酒水」这个徽章，读出了错的数字。

4. **测试要模拟表单真实提交的全部字段**，不能只拼自己关心的那几个参数。
   期间对比的快捷下拉框曾经切了等于没切，就是因为测试直接拼
   `?go=1&preset=30` 没带上表单里会一起提交的 `start/end`。

5. **改了默认值/配色/文案，记得同步 README**，尤其是「全部可配置项一览」那张表。

6. **页面中途 fatal 仍然返回 HTTP 200，内容只是被截断。**
   光看状态码或找某个关键词是抓不到的 —— 必须确认页面**渲染到底**（有 `</body>`）。
   采购页第一版漏了 `require lib/report.php`，用到 `Report::dow()` 时直接
   fatal，页面渲染到第一行就断了，而 curl 拿到的仍是 200。

7. **测试里的日期一律相对今天算，别写死。**
   写死的日期当时是过去，跑到那天之后就成了「未来」，被校验拦下，
   测试自己就红了。这个坑在采购功能上连踩两次。

8. **桩数据要按 SQL 的特征分支，别只看表名。**
   开台列表和营业额统计都查 `order_head`，桩里只写 `str_contains($sql, 'FROM order_head')`
   就会把开台列表的行喂给营业额统计，字段对不上 —— 报出来的是
   「`biz_date` 未定义」，看着像程序 bug，其实是桩写窄了。
   加分支时挑一个**只有那条 SQL 才有**的字段（这里是 `table_name`）。

9. **改了页面就把它加进 `smoke.sh`。**
   `tablecheck.php`（列对齐）和 `responsive.py`（溢出／点击目标／字号）
   都是吃 `smoke.sh` 渲染出来的 HTML 的 —— 页面不在 smoke 名单里，
   这两道检查就等于没跑。新加的子标签第一版高度只有 35px（低于 38px 的
   点击下限），就是靠这条链路抓出来的。

---

## 改动前的自检清单

- [ ] 有没有新增数据库出口？（只允许 `lib/db.php` 和 `lib/store.php` 两个）
- [ ] 有没有可能对**主库**产生写操作？（绝对不行）
- [ ] 写语句是不是只待在 `lib/store.php` / `lib/meat.php` 里？
- [ ] 有没有把主库和自有库放进同一条 SQL（JOIN）？（不行，只能在 PHP 里合并）
- [ ] 新的功能参数写进 `lib/settings.php` 了吗？（不能只写 `config.php`）
- [ ] 新查询有没有 JOIN？扫的是不是大表？要不要默认关掉？
- [ ] 新的表格，数字列表头带 `class="n"` 了吗？
- [ ] 新的状态/趋势，除了颜色还有文字或符号吗？
- [ ] `php tests/selftest.php` 全绿了吗？
- [ ] README 要不要同步改？
